# Legacy analysis scripts

`train_and_opt.py` and `train_opt.py` are retained only for provenance and are
not the canonical R2 reproduction path. They contain historical optimization
settings and must not be used to regenerate the final revised manuscript
results.

For the final revision, use the frozen ANN artifacts in
`ann_optimization_results/` together with the scripts in `code/revision_r2/`.
